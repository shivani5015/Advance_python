# -*- coding: utf-8 -*-
"""Untitled88.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1tW6Zt0VEohtg4xylzbXSkRoe_gYC97Ze
"""

'''
1. What is difference between List and array ?

2. Create an array and calculate shape,size , dimension of an array .

3. What is zeros(), ones(), eye(), diag(), linspace(), unique() . Explain with code ofexample .

4. What is hstack() and vstack() ? Explain with code of example .

5. What is view() vs copy() ?

6. you have to create 2 matrix of shape(2,2) and perfrm matrix multiplication  ?

7. You have to create an array in range of (1-10) you have to create (3x3) matrix of it and then perform total sum , row wise sum , column wise sum  .

8. What is randint(), rand(), seed() in numpy ? Explain with example of code . .'''

#1. What is difference between List and array ?
1.List is hetrogeneous and array is homogeneous.
2.list is mutable and array is immutable.
3.list are slower then arrays.

#2. Create an array and calculate shape,size , dimension of an array .
import numpy as np
a=np.array([[1,2,3],[4,5,6]])
a.shape

a=np.array([[1,2,3],[4,5,6]])
a.size

a=np.array([[1,2,3],[4,5,6]])
a.ndim

#3. What is zeros(), ones(), eye(), diag(), linspace(), unique() . Explain with code ofexample .
#zeros=it creates an array of elements in which all elements are 0.
np.zeros((2,2))

#ones=it creates an array of elements in which all elements are 1.
np.ones((2,2))

#eye=It will create an array in which diagnol position elements are 1 and other are 0.
np.eye(3)

#diag=It will take custom values on diagonal position and others are 0.
a=np.diag([1,2,3])
a

#linspace=it creates an array of same gap values in given range.
np.linspace(0,1,5)

#unique=it will take three array original array, return index and return count.
a=np.array([[1,2,3],[4,5,6]])
np.unique(a,return_index=True,return_counts=True)

#4. What is hstack() and vstack() ? Explain with code of example .
#hstack=it will join array horizontally.
a=np.arange(1,5).reshape(2,2)
b=np.arange(5,9).reshape(2,2)
np.hstack((a,b))

#vstack=it will join arrays vertically.
a=np.arange(1,5).reshape(2,2)
b=np.arange(5,9).reshape(2,2)
np.vstack((a,b))

#5. What is view() vs copy() ?
#view=view means if we change in original it affects in origiinal.
a=np.array([1,2,3,4])
a[0]=8
a

#copy = It creates a new object and chnges in copy does not affect original.
a=np.array([1,2,3,4])
b=a.copy()
b[0]=9
a

#6. you have to create 2 matrix of shape(2,2) and perfrm matrix multiplication  ?
a=np.arange(1,5).reshape(2,2)
b=np.arange(5,9).reshape(2,2)
a.dot(b)

#7. You have to create an array in range of (1-10) you have to create (3x3) matrix of it and then perform total sum , row wise sum , column wise sum  .
c=np.arange(1,10).reshape(3,3)
c

np.sum(c)   #Total sum

np.sum(c,axis=1)  #row wise

np.sum(c,axis=0)  #column wise

#8. What is randint(), rand(), seed() in numpy ? Explain with example of code . .'''
#randint=IT will create a random integer array in a given range.
import random
a=np.random.randint(1,10,2)
a

#rand=It will create a random array in a fixed range 0-1.
b=np.random.rand(6)
b

#seed=it will fixed random generated array.
np.random.seed(1)
np.random.randint(1,5,2)