# -*- coding: utf-8 -*-
"""Untitled50.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1d2RcGrFZLOThJX8osqd8m9zW12IeWFCf
"""

import pandas as pd

df = pd.read_csv("/content/covid_toy - covid_toy.csv")
df.head(2) #top data check return deafults top 5 rows
df.tail(3) #bottam data check return defaults bottam 5 rows

#if we want random indexed data we use df.sample() by default it will give single row
df.sample()

#if we want to check data type of each column then we use df.dtypes
df.dtypes

#if we want to check overall information then we use df.info()
df.info()

#if we want to check stastical view of data we use df.describe()
df.describe()

#if we want to check missing data in df, then we usedf.isnull.sum()
df.isnull().sum()

#we will fill null values by mean
df['fever']=df['fever'].fillna(df['fever'].mean())
df.isnull().sum()

#if you have catogirical data and we have to chcek categories in that then weuse value counts
df['gender'].value_counts()
df['gender']=df['gender'].map({"female":1,"male":0})
df.head(5)

df['cough'].value_counts()
df.sample()
df['cough']=df['cough'].map({"Mild":0,"Strong":1})
df.sample(5)

df['has_covid']=df['has_covid'].map({'Yes':1,'No':0})
df.sample()

df['city'].value_counts()

df['city']=df['city'].map({"Delhi":0,"Mumbai":1,"Bangalore":2,"Kolkata":3})
df.sample()