# -*- coding: utf-8 -*-
"""Untitled69.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1pf4e5ZjwbbuUy2luHKbBlEBJ8TnIppiQ
"""

#Practice set of normal distribution and data dividation

#first we import the libraries to manage data and do calculations on data.
import numpy as np
import pandas as pd

#then we will import the data by using pandas.
df=pd.read_csv("/content/titanic - titanic.csv")
df.head()

# Drop both at once and save the result so my calculation is easy
df = df.drop(columns=['Name', 'Cabin'])

df = df.drop(columns=['Ticket'])

df.head()

#now we will check is there any null values in the dataset
df.isnull().sum()

#so we can see that there is null values in Age column=86 values and fare column =1 so we will
#fill these values by avg value of the column
df['Age']=df['Age'].fillna(df['Age'].mean())
df['Fare']=df['Age'].fillna(df['Fare'].mean())

#now we can again check
df.isnull().sum()

df['Embarked'].value_counts() #checked how many different catogry are their in this column

#now we can convery catogrical column embarked and sex to numerical column for normal distribution
df['Embarked']=df['Embarked'].map({'S':1,'C':2,'Q':3})
df['Sex']=df['Sex'].map({'male':1,'female':2})

df.head()

#now we divide data into input and target data
x=df.drop(columns=['Survived']) #input data
y=df['Survived'] #target data

from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=42)

np.round(x_train.describe(),2)

from sklearn.preprocessing import StandardScaler
sc=StandardScaler()
x_train_sc=sc.fit_transform(x_train)
#This does two things at once to your Training Data:Fit: It calculates the average ($\mu$) and the spread ($\sigma$) for every
#column in your training set.Transform: It uses those values to shift and squeeze your data into that "0 to 1" range.
x_test_sc=sc.transform(x_test)
#This is the most important part. You only use transform on the Test Data.

#The Logic: You use the "ruler" created from the Training set to measure the Test set.

#Why? In the real world, you won't know the average of "future" data. By using the training mean/spread,
#you ensure your model is tested on data scaled exactly the same way it was trained.

#1. Why Scale at all?
#Most machine learning algorithms (like SVM, K-Nearest Neighbors, and Logistic Regression)
#calculate the distance between data points. If the scales are different, the "distance" is distorted.
#Scaling transforms your data so that:

#The Mean becomes 0.

#The Standard Deviation becomes 1.

x_train_new = pd.DataFrame(x_train_sc, columns=x_train.columns)

#we can see that our mean =0 and std=1

np.round(x_train_new.describe(),2)